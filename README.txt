
To run the scanner locally hosted:
1) cd into the folder with the current version of 'UTIC-catalog-number-scanner.html' and copy it to index.html
2) python3 -m http.server   (in a dedicated window)
3) ssh -R 80:localhost:8000 nokey@localhost.run    (in a different dedicated window)


